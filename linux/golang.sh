#!/bin/bash

__install_golang(){
    __yellow_echo "Pending implementation of Golang installation"

    # Create default directories for $GOPATH
    mkdir -p "$HOME/bin"
    mkdir -p "$HOME/pkg"
    mkdir -p "$HOME/src"
}

__install_golang
