#!/bin/bash

__install_terminator(){
    # Terminator
    # Terminator confs
    # ~/.config/terminator/config
    # -b for borderless and
    # -m for maximized.
    # The final Command is following terminator -m -b -l my_default.
    # __install terminator

    return 0;
}

__install_terminator
